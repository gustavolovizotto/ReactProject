# AnimeTracker

SPA em React que consome a API pública [Jikan](https://jikan.moe) (dados do MyAnimeList) para explorar animes e montar uma lista pessoal com status, nota e estatísticas.
Trabalho da disciplina Programação Web Fullstack.

## Integrante

- Gustavo Tesin

## Stack

- Vite + React 18 (JavaScript, `.jsx`)
- Tailwind CSS v4 (`@tailwindcss/vite`)
- React Router v6
- react-hook-form + Yup (`@hookform/resolvers`)
- API: Jikan v4 (`https://api.jikan.moe/v4`), sem chave

## Como rodar

```bash
npm i && npm run dev
```

Outros scripts: `npm run build` (produção), `npm run lint` (oxlint), `npm run preview`.

## Ferramentas de apoio

O [Claude Code](https://claude.com/claude-code) foi usado como ferramenta de apoio no planejamento das tasks, na definição do design e na geração de código. Todo o código gerado foi revisado pelo autor antes de ser incorporado ao projeto.
